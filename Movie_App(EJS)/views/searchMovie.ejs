<!DOCTYPE html>
<html lang="en">

<%- include('partials/head') %>

<body>
    <%- include('partials/navbar') %>
    <div class="container playingNow">
        <div class="row mx-auto">
            <% if (result.length !== 0) {%>
            <h2 class='display-3 header animated bounceInUp' id='header'>you searched for: <span class=' text-success'>"
                    <%= search %>"</span></h2>
            <% result.forEach(movie => {%>
            <a href="/movie/<%= movie.id %>" class='animated zoomInLeft'>
                <div class="col-lg-4 col-md-6 col-sm-12 mx-auto">
                    <div class="card bg-light text-dark" style="width: 18rem;" id="card">
                        <img class="card-img-top img-thumbnail" src="http://image.tmdb.org/t/p/w780/<%= movie.poster_path%>"
                            alt="movie poster">
                        <div class="card-body">
                            <h1>
                                <%= movie.title%>
                            </h1>
                            <small>Release Date:
                                <%= movie.release_date%></small>
                            <p class="card-text">
                                <%= movie.overview.slice(0,100) + ' . . .'%>
                            </p>
                        </div>
                    </div>
                </div>
            </a>
            <%})%>
            <%} else {%>
            <h1 class='display-3 header animated shake' id='headerDown'>Sorry we couldn't find any result for <span
                    class='text-danger'>"
                    <%= search %>"</span>!</h1>
            <%}%>
        </div>
    </div>

    <%- include('partials/footer') %>
</body>

</html>